<UserControl x:Class="OpenRiaServicesProjectDemo.SilverlightApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button x:Name="InsertRecordButton" Grid.Row="0" Click="InsertRecordButton_Click" Margin="0,5,0,0">1. Insert Record</Button>
        <Button x:Name="UpdateRecordButton" Grid.Row="1" Click="UpdateRecordButton_Click" Margin="0,5,0,0">2. Update Record</Button>
        <Button x:Name="DeleteRecordButton" Grid.Row="2" Click="DeleteRecordButton_Click" Margin="0,5,0,0">3. Delete Record</Button>
        <Button x:Name="ExecuteQueryButton" Grid.Row="3" Click="ExecuteQueryButton_Click" Margin="0,5,0,0">4. Run Query</Button>

        <Button x:Name="ExecuteCustomMethodButton" Grid.Row="4" Click="ExecuteCustomMethodButton_Click" Margin="0,20,0,0">Invoke Custom Method</Button>
    </Grid>
</UserControl>
